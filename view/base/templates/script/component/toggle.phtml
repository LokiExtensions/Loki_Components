<script>
    document.addEventListener('alpine:init', () => {
        Alpine.data('LokiToggle', () => ({
            shown: true,
            hidden() {
                return !this.shown;
            },
            toggle() {
                this.shown = !this.shown;
            }
        }));
    });
</script>
